# Handyman Axarquia — site

Rebuilt handymanaxarquia.com (static, EN + SV), generated by `generator/build.py`.

Deployed automatically by Cloudflare Workers Builds on every push to `main`:
https://handmanaxarquia-test.andrewperryuk.workers.dev/

Build command: `pip install pillow qrcode && cd generator && python build.py && python postprocess_prod.py`
